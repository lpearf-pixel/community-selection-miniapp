# Non-secret, isolated GitHub Actions PostgreSQL profile.
# Production values belong in the untracked .env.production and secret store.
POSTGRES_HOST=127.0.0.1
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=community_selection
POSTGRES_SCHEMA=public
