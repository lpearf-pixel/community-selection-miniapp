# L50-C2-T3-B 提现可靠命令实施计划

1. 新增提现命令解析器和请求哈希，先写失败合同。
2. 为 `Withdrawal` 增加 `version`，详情和列表返回当前版本。
3. 新增事务执行器，统一 approve、reject、mark-paid 的状态、关联奖励、账本、审计、业务事件、时间线和回执。
4. 将三个 Admin 路由改接执行器，保持 URL 不变，升级为 V1 响应合同。
5. Admin 提现工作台传递 `expected_version`、独立幂等键和人工说明。
6. 用真实 PostgreSQL 验证同版本并发、幂等重放、跨数据域拒绝与事务回滚。
7. 运行 API 类型、Admin 浏览器和全仓门禁，安全审查后合并。

约束：不实现自动打款，不修改提现申请创建、不扩展退款/库存/配送范围。

## 完成记录

- 完成日期：2026-07-25
- PR：#111
- 精确合并前 head：`9f05d6918bfc9341b378fc00e78e38bfc07109fd`
- 合并提交：`46f5584a7f681a7a0516ed5be5f01a37826e72d8`
- 最终门禁：Verification Baseline Audit run `30169063684` / job `89706894227` 全部通过。
- 安全收口：同团长奖励台账写入按行锁串行化；提现业务事件改为紧凑快照和逐订单佣金集合，避免并发余额错误与平方级事件膨胀。
- 状态：L50-C2-T3-B 已完成并合并；真实自动打款、微信退款、库存调整和配送取消仍不在本任务范围。
