# 管理后台正式登录方案设计

L10 已预留 `ADMIN_AUTH_ENABLED` 与 `x-admin-token`。正式登录建议后续独立阶段实现：

- 使用管理员账号密码或企业身份源登录。
- 登录成功后签发短期会话令牌。
- 后台请求统一携带 Authorization header。
- 管理员操作写 BusinessEventLog。
- 高风险操作二次确认：提现审核、标记已处理、税务复核、告警关闭。
- 当前阶段不实现正式登录，只保留生产前必须启用的令牌保护。
