@import "../../styles/themes/chunhuaqiushi.wxss";

.home-page {
  box-sizing: border-box;
  width: 100%;
  min-height: 100vh;
  padding: 28rpx 28rpx calc(36rpx + env(safe-area-inset-bottom));
  overflow-x: hidden;
}
