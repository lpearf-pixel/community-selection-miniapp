# 社区甄选小程序 Codex 项目规则

## 项目目标

开发一个低开销、稳定可跑的社区甄选微信小程序系统。

业务场景：
- 已有线下实体店
- 已有稳定货源
- 已接入美团
- 已有微信群
- 当前缺少微信小程序
- 需要支持社区团购、开团人一级服务奖励、退款、开团失败处理、自提/配送

第一阶段目标不是大而全商城，而是跑通：

实体店背书 → 微信群种草 → 小程序下单 → 微信支付 → 社区开团 → 自提/配送 → 退款售后 → 一级开团服务奖励。

## 技术选型

必须使用：
- pnpm workspace
- Node.js + TypeScript
- Fastify
- PostgreSQL
- Prisma
- React + Vite + Ant Design 管理后台
- 微信原生小程序
- Docker Compose

第一版不要使用：
- 微服务
- Kubernetes
- Redis
- MQ
- Elasticsearch
- 多级分销系统
- 复杂商城框架

## 目标目录结构

```text
community-selection-miniapp/
├── apps/
│   ├── api/
│   ├── admin/
│   └── miniapp/
├── packages/
│   ├── shared/
│   └── config/
├── prisma/
├── docs/
├── scripts/
├── docker-compose.yml
├── pnpm-workspace.yaml
├── package.json
├── .env.example
├── AGENTS.md
└── README.md
```

## 核心业务规则

1. 只允许一级开团服务奖励。
2. 不允许二级、三级、团队、下级收益。
3. 不允许出现 `parent_leader_id`、`upline_id`、`team_id` 等多级关系字段。
4. 奖励只基于真实有效订单。
5. 退款订单不计算奖励。
6. 部分退款按实际成交金额重新计算奖励。
7. 未成团自动退款，开团人无奖励。
8. 奖励在订单完成后 T+7 变为可提现。
9. 第一版提现走后台人工审核，不做自动打款。
10. 所有页面和字段统一使用“开团服务奖励”，不要使用“返利、分销、下级、团队收益、代理收益”。

## 合规边界

- 不收加盟费、资格费、入门费。
- 不要求购买礼包获得开团资格。
- 不做多级关系。
- 不按发展人数计酬。
- 不宣传躺赚、月入过万、团队收益。
- 奖励只来自开团人本人发起团购下的真实商品成交。

## 金额规则

1. 数据库存储金额统一使用整数分。
2. 不允许使用 float 存储金额。
3. Prisma 中金额字段使用 Int 表示分。
4. 前端展示时格式化为元。

## API 响应格式

成功：

```json
{
  "success": true,
  "data": {},
  "message": ""
}
```

失败：

```json
{
  "success": false,
  "data": null,
  "message": "错误原因"
}
```

## 测试要求

每完成一个阶段，必须运行：

```bash
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

如果命令不存在，先补齐命令。

## 开发方式

不要一次性写完整系统。必须按阶段开发：

1. 项目骨架
2. 数据库模型
3. 商品和后台
4. 团购和订单
5. 微信支付 MOCK
6. 退款
7. 开团服务奖励
8. 自动测试和验收脚本

每阶段完成后必须输出：

1. 完成了哪些功能
2. 修改了哪些文件
3. 执行了哪些测试
4. 测试是否通过
5. 还存在什么问题
6. 下一阶段建议

## 严格约束

不要为了赶进度跳过测试。如果测试失败，先修复测试，不要继续下一阶段。
